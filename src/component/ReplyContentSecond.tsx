// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import React, { useState } from "react";
import {
  PlasmicReplyContentSecond,
  DefaultReplyContentSecondProps,
} from "./plasmic/shared/PlasmicReplyContentSecond";

interface ReplyContentSecondProps extends DefaultReplyContentSecondProps {
  id: string;
  updateId: string;
  timelineId: string;
  organizationId: string;
  userName: string;
  text: string;
  date: string;
}

function ReplyContentSecond(props: ReplyContentSecondProps) {
  const { updateId, timelineId, userName, organizationId } = props;

  const [isReply, setIsReply] = useState<"isReply" | "isUser" | undefined>(
    undefined
  );

  return (
    <PlasmicReplyContentSecond
      state={isReply}
      replyButton={{
        onClick: () => setIsReply("isReply"),
      }}
      replyInput={{
        organizationId: organizationId,
        timelineId,
        updateId,
        userId: userName,
      }}
    />
  );
}

export default ReplyContentSecond;
