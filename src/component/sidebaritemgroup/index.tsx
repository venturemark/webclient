// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from "react";
import {
  PlasmicSidebarItemGroup,
  DefaultSidebarItemGroupProps,
} from "component/plasmic/shared/PlasmicSidebarItemGroup";

// Your component props start with props for variants and slots you defined
// in Plasmic, but you can add more here, like event handlers that you can
// attach to named nodes in your component.
//
// If you don't want to expose certain variants or slots as a prop, you can use
// Omit to hide them:
//
// interface SidebarItemGroupProps extends Omit<DefaultSidebarItemGroupProps, "hideProps1"|"hideProp2"> {
//   // etc.
// }
//
// You can also stop extending from DefaultSidebarItemGroupProps altogether and have
// total control over the props for your component.
interface SidebarItemGroupProps extends DefaultSidebarItemGroupProps {}

function SidebarItemGroup(props: SidebarItemGroupProps) {
  const isCollapsed = false;
  const name = "Venturemark";

  return (
    <PlasmicSidebarItemGroup
      venture={{ name: name }}
      isCollapsed={isCollapsed}
      itemContainer={{}}
      {...props}
    />
  );
}

export default SidebarItemGroup;
