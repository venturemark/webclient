import * as metric from "./metric";
import * as metupd from "./metupd";
import * as update from "./update";

export const API = {
  Metric: metric.Metric,
  MetUpd: metupd.MetUpd,
  Update: update.Update,
};
